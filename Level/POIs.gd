extends Node
#This script holds metadata and describes the behavior for points of interest in a room

var poi_ID : int #Identifier for the poi
var priority : int #Where the wizard should go first
var duration : float #How long it takes for the wizard to interact
var completed = false #Completion state of interaction


func _ready():
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
